services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: destiny
    ports:
      - "5432:5432"
    volumes:
      - destiny_pg:/var/lib/postgresql/data

volumes:
  destiny_pg:
